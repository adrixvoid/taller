import { createTheme } from '@material-ui/core/styles';

const theme = createTheme({
  palette: {
    primary: {
      light: '#c0d1ff',
      main: '#3f51b5',
      dark: '#303f9f',
      contrastText: '#fff',
    },
  },
});

export default theme;
